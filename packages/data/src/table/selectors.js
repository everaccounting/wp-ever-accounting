
export const getItems = (state, endpoint, query) => {
	console.group("Selector");
	console.log(endpoint);
	console.groupEnd();
	// if (hasInState(state, [endpoint])) {
	// 	return state[endpoint];
	// }

	return [];
};
