/* global document, eAccounting */
/**
 * This will hold arbitrary data assigned by the Assets Registry.
 * @type {{}}
 */
const data = window.eAccounting && window.eAccounting.data && window.eAccounting.data || {};

export default data;
