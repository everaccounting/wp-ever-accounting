import data from "./data";

/**
 * Contact types
 * @type {{}}
 */
export const {contact_types: CONTACT_TYPES = {}} = data;

/**
 * Category Types
 * @type {{}}
 */
export const {category_types: CATEGORY_TYPES = {}} = data;

/**
 * Tax rates types
 * @type {{}}
 */
export const {tax_rate_types: TAX_RATE_TYPES = {}} = data;

/**
 * Per Page
 * @type {number}
 */
export const {per_page: PER_PAGE = 20} = data;

/**
 * Payment method
 * @type {*[]}
 */
export const {payment_methods: PAYMENT_METHODS = []} = data;

/**
 * countries
 * @type {*[]}
 */
export const {countries: COUNTRIES = []} = data;
